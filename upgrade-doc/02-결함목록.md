# 02 — 결함 인벤토리 (2026-06-10 전수 감사)

표기: **P0** = 기존 기능을 실제로 깨뜨리거나 데이터를 유실시킴 / **P1** = 조건부 오동작·자원 누수 / **P2** = 품질·죽은 코드·문서 부패.
ID 는 커밋/로그에서 참조한다. 상태 칸은 수정 시 갱신.

## R — 렌더링 / 어댑터

| ID | P | 내용 | 위치 | 상태 |
|---|---|---|---|---|
| R1 | P0 | `applyLayerOverrides` 동시 호출 레이스 — 직렬화 없음, 이미지 디코드 await 사이 인터리브로 페이지별 last-writer 비결정. hydrate 시 오버라이드 N개 → N번 중첩 전체 재구성 | applyOverrides.ts:35–96, LayersPanel.tsx:74–83 | ✅ #34 |
| R2 | P0 | 매 호출 **모든 페이지** 재구성 + 전체 GPU 재업로드 (클린 페이지 포함, 첫 마운트 빈 맵에도). 4096² 페이지당 64MB 캔버스 할당 | applyOverrides.ts:53–95 | ✅ #34 |
| R3 | P0 | Spine `getLayerTriangles` 가 **live attachment** 참조 → 숨긴 레이어(attachment null)는 rect 클립 fallback → destination-out 와이프가 이웃 slot 픽셀 파괴 | SpineAdapter.ts:174, applyOverrides.ts:116–133 | ✅ #35 |
| R4 | P0 | Live2D 멀티페이지 파트: 페이지 분할 Layer 들이 같은 partIdx 의 `partOpacityOverrides` 를 공유 — 한 페이지 숨기면 다른 페이지도 숨고, undo/redo replay 순서에 따라 파트가 되살아남 | Live2DAdapter.ts:428, :613–624; usePuppetMutations.ts:99–105 | ✅ #35 |
| R5 | P0 | `setLayerColor` 와 `setLayerVisibility` 가 같은 맵을 덮어씀 — tint 설정 후 가시성 토글 시 tint 소실, color.a≥1 이 숨긴 파트를 되살림 | Live2DAdapter.ts:613–634 | ✅ #35 |
| R6 | P1 | hydrate 실패 시 `hydratedKeyRef` 미설정 → 세션 내내 저장 silently 중단 | useLayerOverridesPersistence.ts:105–107, :130 | ✅ #36 |
| R7 | P1 | `usePuppet` 언마운트 cleanup 과 in-flight async 의 cancelled 분기가 adapter/app 을 **이중 destroy** | usePuppet.ts:74–77, :93–97, :115–119 | ✅ #36 |
| R8 | P1 | Pixi Assets 캐시 미해제 — puppet 열고 닫을 때마다 GPU 텍스처/비트맵 누수 | SpineAdapter.ts:72–78, :326–333; Live2DAdapter.ts:1049 | ✅ #36 |
| R9 | P1 | mask 합성에 트라이앵글 클립 없음 — rect 안 이웃 픽셀 침식 가능 (런타임 + bakeAtlas 동일) | applyOverrides.ts:147–163, export/bakeAtlas.ts:86–90 | ✅ #34 |
| R10 | P1 | 창 리사이즈 시 baseFactor/중심 재계산 없음 — puppet 이 어긋난 채 방치 | PuppetCanvas.tsx:81, :294–322 | ✅ #36 |
| R11 | P2 | 오버라이드 적용 실패가 console.warn 으로만 — UI 노출 없음. loadBlob null 시 오버라이드 무언 드롭 | LayersPanel.tsx:77, applyOverrides.ts:69–70 | ✅ #34 |
| R12 | P2 | Live2D load 가 동일 manifest 3회 fetch / opacity 곱셈이 엔진 내부 재계산에 암묵 의존 | Live2DAdapter.ts:1041,1084,1151; :572–581 | — |

## E — Decompose Studio / 에디터

| ID | P | 내용 | 위치 | 상태 |
|---|---|---|---|---|
| E1 | P0 | Paint 모드 bucket/wand 가 페인트 캔버스가 아니라 **원본 소스 픽셀**을 BFS — 이번 세션에 칠한 내용 무시 (eyedropper 만 모드 분기됨) | DecomposeStudio.tsx:905–911, :965–967 | ✅ #37 |
| E2 | P0 | Paint 지우개 라운드트립 유실 — `extractCurrentLayerCanvas` 가 와이프 없이 source-over 로 override 를 얹어 재진입/재저장 시 지운 픽셀에 원본 비침 | regionExtract.ts:174–185 (대조: applyOverrides.ts:120–134) | ✅ #37 |
| E3 | P0 | "clear mask" 가 스토어 즉시 쓰기 → 로드 effect 재실행 → 히스토리 전체 + 미저장 paint 작업까지 무경고 파괴, undo 불가 | DecomposeStudio.tsx:1782–1783, :445, :717–725 | ✅ #37 |
| E4 | P0 | Zoom 도구 클릭이 대수적으로 깨진 `zoomAtClient` 사용 (`cx(1-ratio)` 항 소실) — 커서 앵커 안 됨. 올바른 `zoomAroundPoint` 가 바로 아래 있는데 미사용 | useCanvasViewport.ts:125–126 vs :139–159; DecomposeStudio.tsx:1170 | ✅ #37 |
| E5 | P1 | `setZoom` updater 안에서 `setPanX/Y` side effect — StrictMode 에서 pan 2배 | useCanvasViewport.ts:118–131 | ✅ #37 |
| E6 | P1 | embedded(GEN MASK 탭) Esc 가 `onClose` 대신 스토어 `close(null)` 호출 — 부모 탭으로 안 돌아가고 죽은 듯 보임 | DecomposeStudio.tsx:1804–1821 | ✅ #37 |
| E7 | P1 | 모드 간 도구 누설 — paint 전용 eyedropper/SAM 이 단축키로 다른 모드에서 활성화 (보이지 않는 활성 도구) | DecomposeStudio.tsx:1893–1894 | ✅ #37 |
| E8 | P1 | HD-edge 토글이 로드 effect deps 에 있어 미저장 스트로크 무경고 파기 | DecomposeStudio.tsx:380–384, :445 | — |
| E9 | P1 | region IDB hydrate 가 자동 시드/사용자 스트로크를 클로버 (늦은 resolve 시) | DecomposeStudio.tsx:451–493 | — |
| E10 | P1 | worker onerror 후 재생성 없음 — 이후 bucket/wand 클릭 영구 hang. 미 await 라 unhandled rejection | floodFillClient.ts:92–98, DecomposeStudio.tsx:1191,1203 | ✅ #37 |
| E11 | P1 | 히스토리 = 전체 캔버스 클론 ×30, 바이트 상한 없음 (2048²+HD ≈ GB 급) | useHistory.ts:62, DecomposeStudio.tsx:645–656 | — |
| E12 | P1 | split 모드 Ctrl+Z 가 보이지 않는 mask/paint 캔버스를 변조 | DecomposeStudio.tsx:1950–1955, :645–656 | — |
| E13 | P1 | SAM 후보 썸네일 `URL.createObjectURL` 매 렌더 누수 + existingMask URL 미해제 | DecomposeStudio.tsx:2550, :436 | — |
| E14 | P1 | WebGL 컴포지터 region 8개 초과분 무언 비표시 (Canvas2D 는 전부 그림) | compositor.ts:393, :663 | — |
| E15 | P2 | "1:1/Actual size" 가 실제로는 fit 과 동일 (실제 100% 계산 없음) | useCanvasViewport.ts:103–105, OptionsBar.tsx:228–231 | — |
| E16 | P2 | Move 도구(V) 드래그 no-op (카피는 "Drag to pan" 약속) | DecomposeStudio.tsx:1163–1166 | — |
| E17 | P2 | wand 연속 클릭 stale-closure 병합 / StrokeEngine raw 샘플 + 보간 dab 이중 스탬프 / spaceHeld blur 시 고착 | DecomposeStudio.tsx:991–1014; strokeEngine.ts:160; useCanvasViewport.ts:219–222 | — |
| E18 | P2 | 죽은 코드: `applyBrushDab`, sync `floodFill.ts`, `clientToSourcePixel`, `SELECTION_OPS`, zoomAtClient 잔해 등 + 단축키 안내 오기(±5px) | 02-결함목록 참조 | — |
| E19 | P2 | auto-detect 확인창 "취소=뒤에 추가" 트랩 — 진짜 취소 경로 없음 | DecomposeStudio.tsx:1523–1529 | — |
| E20 | P2 | devicePixelRatio 모니터 이동 미감지, 딥줌 백버퍼 업스케일 | DecomposeStudio.tsx:569–583 | — |

## A — AI 파이프라인

| ID | P | 내용 | 위치 | 상태 |
|---|---|---|---|---|
| A1 | P0 | 실행 중 모달 하드락 — running 분기에 버튼 없음 + Esc 차단, 취소 수단 전무 (≤300s 감금). AbortController/취소 라우트 없음 | GeneratePanel.tsx:669–696, :2109–2116; client.ts:848–907 | — |
| A2 | P0 | focus 모드 generate 가 non-OpenAI provider 에서 **무언 no-op** (early return, UI 게이트 없음) | GeneratePanel.tsx:903, :2774–2784 | — |
| A3 | P0 | 폴링 1회 실패 = 전체 실패 throw — 과금된 생성 유실, jobId 재접근 UI 없음 | client.ts:890–892 | — |
| A4 | P0 | 길이 불일치 시 `new Blob()` (0바이트) 시드 → composite 단계에서 전체 실패 | GeneratePanel.tsx:1413–1416 | — |
| A5 | P1 | focus 모드 mask reference 정렬 깨짐 — layer 치수 마스크를 1024² 패딩 island 와 함께 전송하며 프롬프트는 "동일 정렬" 단언 | GeneratePanel.tsx:846 vs :1145–1149; openai.ts:240 | — |
| A6 | P1 | refine 캐시 키가 프롬프트 텍스트뿐 — region 간 오염, refs/negative 변경 미반영 | GeneratePanel.tsx:956 | — |
| A7 | P1 | 결과 blob 서버 1h+ 잔류 — `deleteJob` 미호출, 프루닝은 createJob 안에서만 | jobs.ts:46, :75 | — |
| A8 | P1 | provider fetch 에 서버측 timeout/AbortSignal 없음 (openai/gemini), fal 은 timeout 후 무언 fall-through | openai.ts:144, gemini.ts:104, falai.ts:166–188 | — |
| A9 | P1 | Replicate stub 이 picker 에 노출 — 클릭 = 보장된 실패 | registry.ts:40–51, replicate.ts:66–76 | — |
| A10 | P1 | stale-blend 레이스 — re-blend effect deps 에 `phase` 객체 누락, 새 결과 URL 을 옛 blend 가 revoke/덮어쓰기 가능 | GeneratePanel.tsx:317–344 | — |
| A11 | P1 | 멀티리전 제출이 동일 refs+스냅샷을 N회 중복 업로드, 동시성 캡 없음 / focus 와 전체 generate 의 ref payload 불일치 | GeneratePanel.tsx:1419–1421 vs :993 | — |
| A12 | P2 | 진행률 피드백 없음 (progress 필드 미사용, fal queue_position 폐기, "10–30초" 문구는 실제 60–150s 와 불일치) | types.ts:28, falai.ts:391 | — |
| A13 | P2 | dead code/문서 부패: router.ts 미배선, mask 변환기 2종 미사용, 존재 않는 maskConvert.ts 주석, falai default 가 저품질 flux-pro-fill | router.ts; client.ts:398–465; inpaintMask.ts:144–199 | — |
| A14 | P2 | refine 기본값 고비용 (gpt-5.4 + 전 이미지 detail high) | refine-prompt/route.ts:50, :166–172 | — |

## D — 데이터 / 영속 / Export

| ID | P | 내용 | 위치 | 상태 |
|---|---|---|---|---|
| D1 | P0 | `deletePuppet` 캐스케이드 없음 — 나머지 8개 테이블의 blob(aiJobs PNG 포함) 영구 고아화. 최대 quota 누수원 | db.ts:526–532 | ✅ #36 |
| D2 | P1 | `aiJobs` 무한 성장 — 생성 성공마다 PNG 영속, 프루닝/캡/삭제 UI 없음. `navigator.storage.persist()` 미호출 | GeneratePanel.tsx:1538 | ✅ #36 |
| D3 | P1 | `avatar.json` 이 들어있는 외부 zip 업로드 brick — 스키마 불일치 시 throw 로 전체 업로드 중단 (parseBundle fall-through 없음) | restoreBundle.ts:58–71, page.tsx:202–211 | ✅ #36 |
| D4 | P1 | 썸네일 갱신 → updatedAt bump → re-publish churn 루프 (열기만 해도 30–50MB 재베이크/재전송) | edit/[avatarId]/page.tsx:90–106, db.ts:539–541 | ✅ #36 |
| D5 | P1 | builtin puppet 편집 시 doomed publish 시도 (loadPuppet("builtin:") = null) | db.ts:614, libraryPublisher.ts:252–255 | ✅ #36 |
| D6 | P1 | `onOriginChange` 가 origin 객체 통째 교체 — url/notes 소실 | page.tsx:258 | ✅ #36 |
| D7 | P1 | geny-avatar.zip 라운드트립 유실: animationConfig·references·regionMasks·labels·히스토리·puppet id (re-import = 새 id → Geny 중복 등록) | buildBundle.ts, restoreBundle.ts | — (Stage 6) |
| D8 | P2 | hydrate 완료 전 사용자 편집 유실 가능 / cross-tab last-write-wins | useLayerOverridesPersistence.ts:92–99 | — |
| D9 | P2 | 폴더 드롭/다중 파일 픽의 중첩 번들 실패 (webkitGetAsEntry 미사용, basename fallback 없음) | UploadDropzone.tsx:20–33, rewrite.ts:22–35 | — |
| D10 | P2 | Spine skeleton JSON 휴리스틱 2곳 상이·guessy / parseBundle 소문자 키 충돌 / peekPuppetIdFromZip zip64 미대응 | parseBundle.ts:247, buildModelZip.ts:546; parseBundle.ts:63 | — |
| D11 | P2 | localStorage 기반 publish 북킹 — site data 부분 삭제 시 전 라이브러리 재전송 | libraryPublisher.ts:291–296 | — |

## 수정 순서 원칙

1. **Stage 1 (R1–R5, R9)** — 렌더링 코어. 다른 모든 작업의 토대이고 G3 기반공사(페이지 베이스)와 같은 파일을 만진다. R1/R2 해결 = applyOverrides 재설계 = G3 의 `pageBases` 주입 지점 확보.
2. **Stage 2 (E1–E7, E10)** — 에디터 P0/P1. G2 강화 전에 기존 도구부터 바로잡는다.
3. **Stage 3 (A1–A4, A7–A9)** — Gen 파이프라인 P0. 취소/복구/게이트.
4. **Stage 4~5** — G2/G3 신규 기능 ([03](03-에디터-강화.md), [04](04-전신-리스타일.md)).
5. **Stage 6 (D 군 잔여 + P2)** — 데이터 위생.
