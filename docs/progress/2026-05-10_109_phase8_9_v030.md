# 2026-05-10 — Phase 8.9: v0.3.0 (Animation tab) — Phase 8 종결

[Phase 8 plan](../plan/09_editor_animation_tab.md) 의 마지막 sprint. 8.1~8.8 결과를 README + landing 에 표명 + minor version bump + git tag.

## 변경 surface

### `package.json` — `0.2.4` → `0.3.0`

minor bump (신규 기능 — Animation tab + schema v2). Phase A 의 hotfix 줄 (0.2.x) 과 구분되는 새 능력.

### `app/page.tsx`

landing chip `v0.2.4 · Geny-integratable` → `v0.3.0 · animation tab`. 단독 hobby 사용자도 첫 진입에서 무엇이 새로운지 인지 가능.

### `README.md`

"주요 기능" 섹션에 두 항목 추가:

- **Animation tab** — motion / expression preview, kScale·shift 슬라이더, 8 GoEmotions 매핑, hit-area tap motion 매핑. IDB 영속 + Geny export 자동 동봉.
- **Export / Import** 항목에 baked model zip 의 `avatar-editor.json` schemaVersion 2 sidecar 명시.

## Phase 8 종합 (9 sprint)

| Sprint | commit | 결과 |
|---|---|---|
| 8.1 | tab shell | `[edit\|animation]` 탭 + URL 쿼리 |
| 8.2 | cubismMeta | model3.json 메타 추출 (`useCubismMeta`) |
| 8.3 | DisplaySection | kScale + shift 슬라이더 라이브 |
| 8.4 | MotionsSection | motion ▶ preview |
| 8.5 | ExpressionsSection | expression ▶ + 8 emotion × name 매트릭스 |
| 8.6 | HitAreasSection | 조건부 hit area → motion 매핑 |
| 8.7 | IDB 영속 | Dexie v10 + `usePuppetAnimationConfig` |
| 8.8 | export schemaVersion 2 | `avatar-editor.json` sidecar with animationConfig |
| 8.9 | v0.3.0 + tag | 본 sprint |

## Geny 측 별도 PR (Phase G)

본 phase 와 별개로 Geny 의 install endpoint 갱신 필요 — `avatar-editor.json` 의 schemaVersion 2 인식 + animationConfig 적용:

- `vtuber_baked_imports_controller.install_baked_import` 가 `_peek_zip_metadata` 결과의 `animationConfig` 사용:
  - `display.kScale/initialXshift/initialYshift` → Live2dModelInfo 동일 필드
  - `idleMotionGroupName` → 동일
  - `emotionMap` (NAME 기반) → model3.json 의 expression 순서 파싱 → 인덱스로 변환 → Live2dModelInfo.emotionMap
  - `tapMotions` → `{group: index}` 형식으로 변환 → Live2dModelInfo.tapMotions

별도 sprint — 본 progress 의 범위는 geny-avatar 측 emit 까지.

## 검증

- `pnpm typecheck` 통과
- `pnpm build` 통과
- Phase 8 전체 e2e 시각 검증 가이드:

```bash
git pull && pnpm install && pnpm dev

# 단독 사용:
# 1. /edit/builtin/spineboy → animation 탭 → "Spine 미지원" placeholder
# 2. ellen_joe (또는 expressions/motions 풍부한 puppet) upload
# 3. animation 탭 진입 → 4 sections (display/motions/expressions/hit areas) 모두 채워짐
# 4. ▶ 버튼들 → 캔버스에서 모션 / 표정 즉시 재생
# 5. 슬라이더 / dropdown 변경 → 캔버스 즉시 반영
# 6. 페이지 reload → 변경 사항 IDB 에서 복원
# 7. export model 클릭 → zip 의 avatar-editor.json 안에 schemaVersion 2 + animationConfig 정확히

# Geny 통합 (Phase G 완료 후):
# 8. send to Geny → install → model_registry 에 사용자 매핑 그대로 반영
```

## 다음 — git tag

```bash
git tag -a v0.3.0 -m "v0.3.0 — Animation tab"
git push origin v0.3.0
```

이후 Geny 의 별도 PR (Phase G) — `vendor/geny-avatar` submodule pin v0.2.4 → v0.3.0 + install endpoint 의 schemaVersion 2 처리.
